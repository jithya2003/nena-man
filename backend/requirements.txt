# =============================================================================
# nena-man · backend/requirements.txt
# Core Python dependencies — pinned for reproducibility.
# Run:  pip install -r requirements.txt
# Heavy ML deps (torch, transformers, librosa) are commented out until
# each member is ready to install them for local inference.
# =============================================================================

# --- Web Framework ---
flask==3.1.1
flask-cors==5.0.1

# --- Firebase Admin SDK ---
firebase-admin==6.6.0

# --- Data Science (Lightweight — No GPU needed for stubs) ---
scikit-learn==1.5.2
numpy==1.26.4
pandas==2.2.3
joblib==1.4.2

# --- API Utilities ---
marshmallow==3.23.2
python-dotenv==1.0.1

# --- Testing ---
pytest==8.3.5
pytest-cov==5.0.0

# --- Production WSGI server ---
gunicorn==23.0.0

# =============================================================================
# HEAVY ML DEPENDENCIES — Uncomment ONLY when your Colab-trained model
# is ready to be deployed locally for inference.
# =============================================================================

# --- M1: Speech Classifier (KULATHILAKA — IT23175198) ---
# torch==2.3.1
# torchaudio==2.3.1
# transformers==4.44.2
# librosa==0.10.2
# soundfile==0.12.1

# --- M2/M3/M4: Advanced ML ---
# xgboost==2.1.1
# shap==0.46.0
